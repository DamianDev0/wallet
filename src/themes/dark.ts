
import { darkColors as colors } from './colors/colors';
import { typography } from './config/typography';
import { spacing } from './config/spacing';
import { sizes } from './config/sizes';

export const darkTheme = {
  colors,
  typography,
  spacing,
  sizes,
};
